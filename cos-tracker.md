# Chief of Staff, Improvement Tracker

Running backlog of tweaks, improvements, and architecture changes for the Frey Family Assistant (freyfam-cos). Items are captured as they come up and sequenced later.

## Backlog

| ID | Item | Phase | Status | Notes |
|----|------|-------|--------|-------|
| 001 | Migrate **Chef Patrick** and **Shea** agents off the orchestrator onto their own dedicated local hardware (one Mac mini or Microsoft mini PC each). Reserve the tenant for the **COO specialist**. | Long-term | Not started | Today these run as in-process personas on the MacBook daemon. Goal is dedicated local nodes per agent, with the tenant kept clear for the COO specialist. Open questions before implementation: node type (Mac mini vs. Microsoft mini), how the orchestrator reaches them across machines (local endpoints / message queue), and how memory + approval flows route when agents aren't in-process. |
| 002 | Revive the **CVS ordering / routing program** carried over from the old Family Assistant. | Near-term | Not started | Pharmacy ordering + routing logic exists from the prior build. Needs to be located, dusted off, and ported into the freyfam-cos structure. |
| 003 | Stand up the ability for **Lloyd** to log into sites and use the internet (authenticated browsing). | In progress | Parallel track | A parallel task already covers this (the Chromebook + Claude for Chrome supervised browser layer). Flagged as a must-land item so it doesn't stall. |
| 004 | Have **Lloyd** surface upcoming-week scheduling **conflicts in the weekly digest**, not just the morning digest. | Near-term | Not started | Forward-looking so there's lead time to adjust or reschedule, rather than discovering the clash the morning of. Weekly digest should scan the week ahead for overlaps and flag them. |
| 005 | Replace all **Bright Horizons** daycare references with **Woodbury Preschool Village** across the assistant. | Near-term | Not started | Two parts: (a) immediate, stop all Bright Horizons content from appearing in the digest; (b) pending, load Woodbury's curriculum and menu once Nic provides them. Part (a) can happen now; part (b) is blocked on Nic. |
| 006 | Have **Frank** tap into the **Ring ecosystem** for movement/occupancy tracking, to learn household patterns and optimize house control, energy efficiency, thermal management, and security. | Long-term | Research first | Exploratory. First unknown is API access: Ring has no robust official public API and Amazon has periodically restricted third-party access, so viability depends on unofficial libraries (e.g. ring-client-api) or an alternative. Worth weighing whether dedicated presence/occupancy sensors would give cleaner signal than Ring motion events. Pattern-learning layer (occupancy over time to drive HVAC/security) is a separate build on top of whatever data source wins. |
| 007 | Set up **iMessage** as a channel. | Near-term | Not started | No official iMessage API. On the MacBook daemon the realistic paths are AppleScript automation of Messages.app or a self-hosted bridge (e.g. BlueBubbles) that exposes send/receive + webhooks. Decide which before building; a bridge is more robust for two-way flow. |
| 008 | Set up **grocery ordering and planning**. | Near-term | Not started | Ties back to earlier meal-planning work (Google Reminders + Ralphs app). Planning side is straightforward; ordering automation depends on what the grocer exposes: Kroger/Ralphs API is limited for cart/checkout, so this may lean on the authenticated-browsing layer (item 003). Sequence after or alongside 003 if no usable API. |
| 009 | **Email drafting voice rule.** When **Lloyd** writes an email into Nic's draft inbox, it defaults to Nic's voice (first person, as if Nic wrote it). Only when Nic explicitly asks for an email *from Lloyd* should it be written in Lloyd's voice. | Near-term | Not started | Behavior rule for the email-drafting path. Default = ghostwrite as Nic; override = write as Lloyd on explicit request. Applies to anything landing in the draft inbox. |
| 010 | **Bug:** In-app **mute** in the Lloyd web app doesn't actually mute. It's meant to stop the mic from picking up noise, but the microphone keeps capturing and the audio keeps being processed; mute only changes the UI state. | Bug / fix | Ready to fix | Root cause is app logic, not OS/PWA settings: the mute toggle sets a flag but never gates the audio. Fix direction: on mute, either set the getUserMedia audio track's `enabled = false` (keeps the session, sends silence) or stop the track entirely (also clears the OS mic indicator) and re-acquire on unmute, and short-circuit the VAD/speech-processing step so it doesn't run on muted input. Prefer stopping the track if the goal is truly "mic off." |
| 011 | **Reduce approval friction for calendar events** created from Nic's and Shelli's work emails. Today every event triggers two approval emails; target is at most one, and no secondary pre-approval in many cases. | Near-term | Not started | Two parts: (a) collapse the double approval to a single gate (first determine why there are two today, e.g. a triage approval plus a write approval, so we know which to remove); (b) define auto-create criteria so routine, low-stakes events skip approval entirely, e.g. explicit date/time, trusted sender, and no calendar conflict. Reserve the single approval for ambiguous, conflicting, or high-stakes events. Conflict check can reuse item 004's logic. Note: writing to the freyfam.com "Nic Work" calendar is a write to Nic's own calendar and does not violate the inbound-read-only rule on the work domains (that rule governs replying to work mail, not calendar writes). |

## Notes / parking lot

_(Cross-cutting ideas, dependencies, and decisions that aren't yet discrete tasks go here.)_

- Item 003 overlaps an existing parallel effort; confirm whether "Lloyd logging into things" is fully covered by the Chromebook/Claude-for-Chrome layer or needs additional auth/credential handling.
- **Owed by Nic:** Woodbury Preschool Village curriculum and menu items (unblocks item 005b).
- **Nic action item:** Review the existing "Chief of Staff Family" tracker inside the Claude Code project and reconcile still-open items against this backlog. (This Claude can't see that repo from here. Paste the open items and I'll merge them in and de-dupe against 001-008.)
